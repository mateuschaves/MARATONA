#include <stdio.h>
#include <stdlib.h>

int main(void){
        int n, k, i, o = 1;
        scanf("%d", &n);
        i = n - 1;

        // Criando um vetor para armazenar alguma coisa ai.
        int v[n];

        // Numerando o vetor.
        for(int x = 0; x <= n; x++ ){
            v[x] = x + 1;
        }
        
        while(1){
            // Preciso que isso funcioneeeeeeeeeeeeeeeeeeeeeee
            
        }

        system("pause");
}